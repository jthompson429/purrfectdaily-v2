{
  "name": "WeightLog",
  "type": "object",
  "properties": {
    "workspace_id": {
      "type": "string",
      "description": "Workspace this weight log belongs to"
    },
    "pet_id": {
      "type": "string"
    },
    "weight": {
      "type": "number"
    },
    "unit": {
      "type": "string",
      "enum": [
        "g",
        "kg",
        "lb"
      ],
      "description": "Unit used when the weight was recorded"
    },
    "date": {
      "type": "string",
      "format": "date"
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [
    "pet_id",
    "weight",
    "date",
    "workspace_id"
  ],
  "rls": {
    "create": {
      "data.__rls_block": true
    },
    "read": {
      "data.workspace_id": {
        "$in": "{{user.data.workspace_ids}}"
      }
    },
    "update": {
      "data.__rls_block": true
    },
    "delete": {
      "data.__rls_block": true
    }
  }
}